{% extends 'base.html.twig' %}

{% block title %}CommercialEntity{% endblock %}

{% block body %}
    <h1>CommercialEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commercial_entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ commercial_entity.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ commercial_entity.type }}</td>
            </tr>
            <tr>
                <th>Success</th>
                <td>{{ commercial_entity.success ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commercial_entity_index') }}">back to list</a>

    <a href="{{ path('commercial_entity_edit', {'id': commercial_entity.id}) }}">edit</a>

    {{ include('commercial_entity/_delete_form.html.twig') }}
{% endblock %}
