{% extends 'base.html.twig' %}

{% block title %}InternalEntity{% endblock %}

{% block body %}
    <h1>InternalEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ internal_entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ internal_entity.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ internal_entity.type }}</td>
            </tr>
            <tr>
                <th>Success</th>
                <td>{{ internal_entity.success ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('internal_entity_index') }}">back to list</a>

    <a href="{{ path('internal_entity_edit', {'id': internal_entity.id}) }}">edit</a>

    {{ include('internal_entity/_delete_form.html.twig') }}
{% endblock %}
